# OPC UA via MQTT Extension - Provider Broker Configuration
# Add these variables to your provider connector environment file
# Location: deployment/assets/env/provider_connector.env

# ============================================================
# MQTT Broker Configuration (Provider-Managed)
# ============================================================

# Required: MQTT Broker URL
# Format: tcp://host:port or ssl://host:port
# Example for local development:
EDC_OPCUA_MQTT_BROKER_URL=tcp://localhost:1883

# Optional: MQTT Broker Username
# Leave empty or omit if broker allows anonymous connections
# EDC_OPCUA_MQTT_USERNAME=mqtt_user

# Optional: MQTT Broker Password/Token
# Leave empty or omit if broker allows anonymous connections
# EDC_OPCUA_MQTT_PASSWORD=mqtt_password

# ============================================================
# Notes
# ============================================================
#
# 1. The MQTT broker should be running before starting the connector
#    See deployment/mqtt/docker-compose-mqtt.yml for local setup
#
# 2. Credentials are optional if broker allows anonymous connections
#    - EMQX default: allows anonymous (configure with EMQX_ALLOW_ANONYMOUS=true)
#    - Mosquitto: configure in mosquitto.conf (allow_anonymous true)
#
# 3. For production, use TLS (ssl://) and strong credentials:
#    EDC_OPCUA_MQTT_BROKER_URL=ssl://mqtt-broker.example.com:8883
#    EDC_OPCUA_MQTT_USERNAME=provider-service
#    EDC_OPCUA_MQTT_PASSWORD=<strong-password>
#
# 4. The broker URL and credentials are used by the provider connector
#    to publish OPC UA sensor data to the MQTT topics
#
# 5. Consumers will receive separate MQTT broker credentials via
#    the transfer process (implementation-specific)
#

